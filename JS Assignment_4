//Question 1
let prompt=require('prompt-sync')();

function matchHouses(step)
{
    return (4*step)-(step-1)+(step*2);
}
let step = prompt('Enter the number of matches ')
console.log(matchHouses(step))
